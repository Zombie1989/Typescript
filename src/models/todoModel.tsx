export interface Todo {
    id: number
    text: string
    isCompleted: boolean
}

export interface Iproduct {
    name: string
    imageName: string
    update: string
    delete: boolean
    price: number
}